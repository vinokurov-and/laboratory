# GitHub, как облачная платформа

## 1. Введение

GitHub когда-то был просто местом, где хранили код — `git push, git pull`, обмен файлами.
Сегодня — это мощная облачная платформа, которая берёт на себя:
- хостинг приложений (GitHub Pages),
- автоматизацию сборки и тестов (GitHub Actions),
- управление доступом, уведомлениями и безопасностью.

Для фронтенд-разработчика — это идеальная среда.
Потому что React-приложение — это статика.
Статика — это то, что GitHub Pages умеет делать лучше всех.

💡 Важно: Ты не разворачиваешь Node.js-сервер. Ты собираешь файлы HTML, CSS и JavaScript, собранные в папку build/. И GitHub трансформирует это в живой сайт — без твоего участия.

## 2. Github Pages
GitHub Pages — бесплатный хостинг статических файлов, встроенный в GitHub.
Ты загружаешь файлы — GitHub отдаёт их по HTTP с SSL, кэшированием и CDN.

Форматы URL
  - https://<username>.github.io/<repository-name>
  - https://<custom-domain.com> (при настройке кастомного домена)

## 3. Github Actions
Ты разрабатываешь React-приложение.
Ты вносишь изменения → запускаешь npm test → собираешь build → заливаешь в GitHub Pages.
Это — рутина.
Ты не должен делать это каждый раз.

🔹 Что такое GitHub Actions?
Это облачная система автоматизации, которая выполняет задачи по триггерам:

push ->	Ты закоммитил изменения в main → запускается сборка
pull_request ->	Ты открыл PR → запускаются тесты, чтобы не сломать основную ветку
schedule ->	Каждый день в 3:00 — отправляется уведомление или резервная копия

Представь:
Ты вносишь изменение в файл src/components/Button.js → коммитишь → и GitHub автоматически:
1. Скачивает твой код
2. Устанавливает Node.js
3. Запускает npm install
4. Запускает npm test — если тесты падают — тебе приходит уведомление
5. Если всё хорошо — запускает npm run build
6. Загружает build/ на GitHub Pages
7. Отправляет тебе уведомление: «✅ Деплой прошёл! Посмотри: твой url»
→ Ты не делаешь ничего.
→ Ты пишешь код.
→ Облако заботится о том, чтобы твой код работал всегда.

Всё это описывается в файле .github/workflows/ci.yml — YAML, простой, как конфиг.
Пример:
```yaml
name: Build and Deploy
on: [push]

jobs:
  test-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - run: npm ci
      - run: npm test -- --watchAll=false  # запустить тесты
      - run: npm run build                 # собрать React
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build
```


## Рекомендации к просмотру и чтению

- [Github Actions - Официальная документация](https://docs.github.com/ru/actions)
- [Хабр: Github Actions](https://habr.com/ru/articles/711278/)
- О [GitHub Actions](https://www.youtube.com/watch?v=e0A2hDObLmg)
- [Курс по GitHub Actions](https://www.youtube.com/watch?v=nhenfObKQvU)
