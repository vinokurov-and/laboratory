# Автоматизированное тестирование в облаке — GitHub Actions как защита кода

## ❌ Проблема без автоматизации
- Ты меняешь Button.js → локально всё работает.
- Забываешь запустить npm test.
- Коммитишь в main.
- Через час кто-то пишет: «Сайт сломался!»
- Ты — виноват.
- → Это не про невнимательность. Это про отсутствие защиты.

## ✅ Решение: Тесты + CI в облаке
- Каждый коммит → автоматически запускаются тесты.
- Если тесты падают → деплой не происходит, мёрдж блокируется.
- Ты не можешь сломать то, что работает.

## 🧪 Типы тестов в React-приложениях
| Тип теста      | Что проверяет | Что проверяет |
| -------------  | ------------- | ------------- |
| Unit-тесты     | Отдельные функции, хуки, утилиты  | useAuth() возвращает правильный статус  |
| Snapshot-тесты | Внешний вид компонента (HTML-структура)  | `<Button>` должен всегда отображать “Нажми меня”  |
| Интеграционные | Взаимодействие компонентов | `<App>` → `<Header>` → `<Button>` — всё работает вместе |

🔧 Пример теста в src/App.test.js (из create-react-app)
```javascript
import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  render(<App />);
  const linkElement = screen.getByText(/learn react/i);
  expect(linkElement).toBeInTheDocument();
});
```

✅ Запуск в терминале:
```
npm run test
```

## ⚙️ GitHub Actions: автоматизация тестов
🔹 Что делает GitHub Actions?
Это облачная машина, которая:

Скачивает твой код
Устанавливает Node.js
Запускает npm ci
Запускает npm test
Говорит: «Прошёл» или «Не прошёл»

### Пример конфигурации
github/workflows/test.yml
```
name: Run React Tests
on: [push, pull_request]  # Запускать при каждом коммите и PR

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3          # Скачать код
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18                 # Установить Node.js 18
      - run: npm ci                        # Установить зависимости (быстрее, чем npm install)
      - run: npm test -- --watchAll=false  # Запустить тесты (не в режиме наблюдения!)
```

## 🔍 Что происходит, когда ты делаешь git push?
1. GitHub Action запускается.
2. Создаётся виртуальная Ubuntu-машина.
3. Скачивается твой код.
4. Устанавливается Node.js.
5. Запускаются тесты.
6. Ты видишь результат в вкладке Actions на GitHub:
🟢 Зелёный ✅ — всё хорошо
🔴 Красный ❌ — тест упал → нужно исправить

💡 Это — тестирование в чистой среде.
Ты не тестируешь “у меня работает”.
Ты тестируешь “на новом компьютере, без кэша, без ошибок — работает ли?”.

## 🛡️ Защита ветки main — главный инструмент безопасности
Ты написал тесты.
Ты настроил GitHub Actions.
Теперь — защити свою ветку.

🔧 Как запретить мёрдж, если тесты не прошли:
1. Перейди в репозиторий на GitHub → Settings → Branches
2. Нажми Add rule
3. Заполни:
- Branch name pattern: main
- ✅ Require status checks to pass before merging
- ✅ Include administrators (даже ты не сможешь обойти!)
- В списке Status checks → выбери: Run React Tests
4. Нажми Create

## ⚠️ Что происходит дальше?
Ты создаешь ветку feature/new-button
Делаешь изменения → ломаешь тест
Открываешь Pull Request
GitHub показывает: “Required status check ‘Run React Tests’ is failing.”
Кнопка “Merge” неактивна.
Ты возвращаешься к коду, исправляешь код или тест → снова пуш → всё проходит → мёрдж разрешён.

## Почему это важно?
| Что учат в вузах      | Что делают работодатели |
| -------------  | ------------- |
| “Пишите тесты, это хорошо”     | “Если тесты не проходят — код не попадает в прод”  |
| “Пушьте в main” | “main — это продакшн. Там не должно быть багов”  | 
| “Всё работает у меня локально” | “Проверь в CI. Иначе ты сломаешь всем” |
